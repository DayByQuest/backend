spring:
  config:
    import:
  profiles:
    group:
      local:
        - data
        - web
        - aws
      prod:
        - prod-data
        - web
        - aws
      performance:
    default: local